# Data (never commit the big files)
data/raw/*
data/interim/*
data/processed/*
data/external/*
!data/**/.gitkeep
*.zip

# Model artifacts / caches
models/*
!models/.gitkeep
reports/figures/*
!reports/figures/.gitkeep
*.pkl
*.joblib
*.bin
*.pt
*.onnx

# Submissions (keep the folder, ignore the csvs by default)
submissions/*.csv

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
.ipynb_checkpoints/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
build/
dist/

# Secrets / local
.env
*.local
kaggle.json

# Export Kaggle (régénéré, ne pas committer — gros parquets)
scripts/kaggle/dataset/
scripts/kaggle/features_dataset/
scripts/kaggle/kernel-metadata.json
scripts/kaggle/**/dataset-metadata.json

# OS
.DS_Store
Thumbs.db
