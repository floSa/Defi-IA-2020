# Core (CPU) — étapes A/B : data, features, GBM, évaluation
numpy>=1.26
pandas>=2.1
pyarrow>=15.0            # I/O parquet
scipy>=1.11
scikit-learn>=1.4
lightgbm>=4.3
catboost>=1.2
networkx>=3.2            # arbre de réponses / graphe d'auteurs
nltk>=3.8               # VADER, tokenisation stylométrique
pyyaml>=6.0
tqdm>=4.66
joblib>=1.3
click>=8.1              # CLI

# Étapes C/D (GPU) — installées séparément selon l'environnement (voir requirements-gpu.txt)
